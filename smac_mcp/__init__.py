"""The SMAC MCP bridge: a pure HTTP client package with no imports from `app/`.

Lets an MCP client (e.g. Claude Desktop) join a SMAC workspace as an agent
member, over the same front door every other client uses.
"""
