# Dependencies for the analyst_agent example ONLY -- never merged into the
# server's top-level pyproject.toml. Install with:
#   pip install -r examples/analyst_agent/requirements.txt
anthropic>=0.40.0
httpx>=0.27.2
websockets>=17.0.1
textual>=8.2.8
python-dotenv>=1.2.2
