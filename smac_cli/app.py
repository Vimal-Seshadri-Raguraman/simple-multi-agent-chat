"""The `smac` TUI client entry point.

Stub for this task: the `smac = smac_cli.app:main` console script must
resolve (so `pip install -e .` exposes both scripts and mypy has a target
to check) even though the Textual TUI itself arrives in a later task on
this branch.
"""


def main() -> None:
    """Entry point for the `smac` console script. Not yet implemented."""
    raise SystemExit("smac TUI arrives in a later task on this branch")
